<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SyncTrip.Mobile.Features.Authentication.ViewModels"
             x:Class="SyncTrip.Mobile.Features.Authentication.Views.MagicLinkPage"
             x:DataType="vm:MagicLinkViewModel"
             Title="Connexion"
             Shell.NavBarIsVisible="False">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

            <!-- Logo / Title -->
            <Label Text="SyncTrip"
                   FontSize="42"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   Margin="0,40,0,20" />

            <Label Text="Synchronisez vos convois"
                   FontSize="18"
                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}"
                   HorizontalOptions="Center"
                   Margin="0,0,0,40" />

            <!-- Email Entry -->
            <Label Text="Adresse email"
                   FontSize="14"
                   FontAttributes="Bold" />

            <Frame BorderColor="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray600}}"
                   CornerRadius="10"
                   Padding="0"
                   HasShadow="False">
                <Entry Placeholder="votre@email.com"
                       Text="{Binding Email}"
                       Keyboard="Email"
                       ReturnType="Send"
                       ReturnCommand="{Binding SendMagicLinkCommand}"
                       Margin="15,0" />
            </Frame>

            <!-- Send Button -->
            <Button Text="Envoyer le lien magique"
                    Command="{Binding SendMagicLinkCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                    HeightRequest="50"
                    CornerRadius="25"
                    FontAttributes="Bold"
                    BackgroundColor="{StaticResource Primary}"
                    Margin="0,10,0,0" />

            <!-- Loading Indicator -->
            <ActivityIndicator IsVisible="{Binding IsBusy}"
                              IsRunning="{Binding IsBusy}"
                              Color="{StaticResource Primary}"
                              HeightRequest="40" />

            <!-- Message -->
            <Frame IsVisible="{Binding Message, Converter={StaticResource IsNotNullOrEmptyConverter}}"
                   BorderColor="{Binding IsSuccess, Converter={StaticResource SuccessErrorColorConverter}}"
                   BackgroundColor="Transparent"
                   Padding="15"
                   CornerRadius="10"
                   HasShadow="False">
                <Label Text="{Binding Message}"
                       TextColor="{Binding IsSuccess, Converter={StaticResource SuccessErrorColorConverter}}"
                       HorizontalOptions="Center"
                       HorizontalTextAlignment="Center" />
            </Frame>

            <!-- Info -->
            <Label Text="Vous recevrez un email avec un lien de connexion sécurisé."
                   FontSize="12"
                   TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray400}}"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"
                   Margin="0,20,0,0" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
